# homebrew-tap

Homebrew tap for [gnt.ai](https://gnt.ai).

```
brew install gnt-ai/tap/gnt
```

Wraps the published npm package ([`@gnt-ai/cli`](https://www.npmjs.com/package/@gnt-ai/cli)), not a separate binary distribution, just a Homebrew-friendly way to get it installed.
